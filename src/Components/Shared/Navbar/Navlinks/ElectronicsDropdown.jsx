import { NavLink } from "react-router-dom";

const ElectronicsDropdown = () => {
  return (
    <div className="grid grid-cols-2 lg:flex flex-row items-start gap-12 ">
      <div className="pl-4 space-y-1">
        <h2 className="text-lg font-medium mb-3 text-red-500">Smart Phones</h2>
        {/* products 1  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Apple
          </NavLink>
        </div>
        {/* products 2  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Samsung
          </NavLink>
        </div>
        {/* products 3  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Oppo
          </NavLink>
        </div>
        {/* products 4  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Infinix
          </NavLink>
        </div>
        {/* products 5  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Vivo
          </NavLink>
        </div>
      </div>
      <div className="pl-4 space-y-1">
        <h2 className="text-lg font-medium mb-3 text-red-500">Head Phones</h2>
        {/* products 1  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Apple
          </NavLink>
        </div>
        {/* products 2  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Samsung
          </NavLink>
        </div>
        {/* products 3  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Oppo
          </NavLink>
        </div>
        {/* products 4  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Infinix
          </NavLink>
        </div>
        {/* products 5  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Vivo
          </NavLink>
        </div>
      </div>
      <div className="pl-4 space-y-1">
        <h2 className="text-lg font-medium mb-3 text-red-500">Apple MacBook</h2>
        {/* products 1  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Apple
          </NavLink>
        </div>
        {/* products 2  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Samsung
          </NavLink>
        </div>
        {/* products 3  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Oppo
          </NavLink>
        </div>
        {/* products 4  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Infinix
          </NavLink>
        </div>
        {/* products 5  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Vivo
          </NavLink>
        </div>
      </div>
      <div className="pl-4 space-y-1">
        <h2 className="text-lg font-medium mb-3 text-red-500">Watch</h2>
        {/* products 1  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Apple
          </NavLink>
        </div>
        {/* products 2  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Samsung
          </NavLink>
        </div>
        {/* products 3  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Oppo
          </NavLink>
        </div>
        {/* products 4  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Infinix
          </NavLink>
        </div>
        {/* products 5  */}
        <div className="text-sm text-black font-medium">
          <NavLink
            to="/products"
            className={({ isActive, isPending }) =>
              isPending
                ? "pending"
                : isActive
                ? ""
                : "cursor-pointer hover:text-red-600"
            }
          >
            Vivo
          </NavLink>
        </div>
      </div>
    </div>
  );
};

export default ElectronicsDropdown;
